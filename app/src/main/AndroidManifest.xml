<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.peerreview.bengalifm"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="15" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name="in.peerreview.bengalifm.SplashActivity"
            android:label="@string/title_activity_music_android" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="in.peerreview.bengalifm.MusicAndroidActivity"
            android:label="@string/title_activity_music_android" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <service android:enabled="true"  android:name="in.peerreview.bengalifm.BackgroundSoundService"/>
        <receiver  android:name="in.peerreview.bengalifm.MyAlarm">
        </receiver>

        <receiver android:name="SchedulerSetupReceiver">
            <intent-filter >
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>